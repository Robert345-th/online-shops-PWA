{
  "name": "zedmarket-android",
  "private": true,
  "scripts": {
    "sync": "bubblewrap update --manifest twa-manifest.json --skipPwaValidation --skipVersionUpgrade",
    "patch": "node patch-webview-only.js && node patch-offline-shell.js && node patch-webview-location.js && node patch-version.js",
    "build": "npm run sync && npm run patch && bubblewrap build --skipPwaValidation && node copy-apk.js",
    "build:aab": "npm run sync && npm run patch && bubblewrap build --skipPwaValidation",
    "fingerprint": "bubblewrap fingerprint list"
  },
  "devDependencies": {
    "@bubblewrap/cli": "^1.22.0"
  }
}
